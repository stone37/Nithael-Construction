{% extends 'site/layout.html.twig' %}

{% block title 'Services' %}

{% set settings = config.get %}

{% block meta %}
    {% set content = "Découvrez nos services immobiliers sur mesure. Achats, ventes, investissements : nous sommes là pour concrétiser vos projets. Parcourez nos offres dès maintenant !" %}
    <meta name="description" content="{{ content }}"/>
    <meta property="og:description" content="{{ content }}" />
{% endblock %}

{% block app_intro %}
    {{ parent() }}

    <section class="bc container">
        {{ wo_render_breadcrumbs({
            viewTemplate: 'ui/_breadcrumb.html.twig',
            listClass: 'breadcrumb',
            itemClass: 'breadcrumb-item',
            separator: '>',
            separatorClass: 'fas fa-angle-right'}) }}
    </section>
{% endblock %}

{% block app_body %}
    {{ include('site/pages/service/_content.html.twig') }}
{% endblock %}

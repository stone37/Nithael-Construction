{% extends 'admin/layout.html.twig' %}

{% block title %}Tableau de bord | {{ parent() }}{% endblock %}

{% block app_body %}
    {{ include('admin/dashboard/index/_header.html.twig') }}

    {{ include('ui/_flashes_message.html.twig') }}

    {{ include('admin/dashboard/index/_content.html.twig') }}
{% endblock %}


{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript" defer>
        const $oDaily = '{{ days|json_encode|raw }}',
            $oMonthly = '{{ months|json_encode|raw }}';
    </script>

    {{ include('ui/_javascripts.html.twig', {'path': 'assets/js/admin/stats.js'}) }}
{% endblock %}

